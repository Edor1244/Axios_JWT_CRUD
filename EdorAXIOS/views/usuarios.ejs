<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Usuarios</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css"
        href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">
    <style>
        /* Estilos para el cuerpo de la página */
        body {
            background: linear-gradient(to right, #f8f9fa 0%, #e9ecef 100%);
            font-family: 'Open Sans', sans-serif;
            color: #343a40;
        }

        /* Estilos para la barra de navegación */
        .navbar {
            background-color: #343a40;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            display: flex;
            /* Contenedor principal con flexbox para centrar el contenido */
            justify-content: center;
            /* Elementos centrados horizontalmente */
            align-items: center;
            /* Elementos centrados verticalmente */
            padding: 10px 20px;
            /* Espacio interno */
        }

        .navbar-dark .navbar-nav .nav-link {
            color: #ffffff;
            text-decoration: none;
            /* Quitar subrayado del texto */
            padding: 10px 15px;
            /* Espacio entre enlaces */
        }

        .navbar-dark .navbar-nav .nav-link:hover {
            color: #00FF00;
            /* Cambio de color al pasar el mouse */
        }

        .nav-link.active {
            font-weight: bold;
            color: #00FF00;
            /* Color para el enlace activo */
        }

        .navbar-brand {
            font-weight: bold;
            color: #00FF00;
        }

        /* Botón con degradado */
        .navbar-brand,
        .nav-link {
            background-image: linear-gradient(to right, #007bff 0%, #0056b3 50%);
            /* Degradado azul */
            transition: background-color 0.3s ease;
            /* Animación del degradado */
        }

        .navbar-brand:hover,
        .nav-link:hover {
            background-color: #0056b3;
            /* Color al pasar el mouse sobre el botón */
        }

        /* Estilos para el título */
        h1 {
            font-family: 'Roboto Slab', serif;
            font-size: 2.5rem;
            color: #321433  ;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
            text-align: center;
            /* Centrar el título */
            position: relative;
            /* Posicionamiento relativo para la animación */
        }

        h1 span {
            display: inline-block;
            /* Convertir cada letra en un bloque en línea */
            animation: letter-appear 1s ease-in-out forwards;
            /* Animación por letra */
        }

        @keyframes letter-appear {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }

            50% {
                transform: translateY(-10px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }



        /* Estilos para el formulario */
        form {
            background-color: #fff;
            /* Use shorthand for white */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
            /* Subtle shadow */
            transition: all 0.3s ease-in-out;
            /* Smooth transition for hover */
        }

        form:hover {
            box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.2);
            /* Enhanced shadow on hover */
        }

        .form-label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        .form-control {
            flex: 1;
            /* Inputs occupy remaining space */
            padding: 8px 12px;
            /* Adjust padding for better spacing */
            border: 1px solid #ccc;
            border-radius: 4px;
            transition: border-color 0.3s ease;
            /* Animation for focus */
        }

        .form-control:focus {
            border-color: #007bff;
        }

        /* Improved animation on focus */
        .form-control:focus {
            outline: none;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.3);
            /* Blue shadow on focus */
        }

        /* Button animation on hover */
        .btn-primary:hover {
            transform: translateY(-2px);
            /* Slight upward movement */
            transition: transform 0.2s ease;
            /* Faster animation */
        }

        .btn-primary:active {
            transform: translateY(0px);
        }

        /* Estilos para la tabla de empleados */
        .table {
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            /* Ancho completo para que ocupe toda la pantalla */
        }

        .table th,
        .table td {
            border: 1px solid #ddd;
            padding: 8px;
            /* Espacio interno en las celdas */
        }

        .table tr:nth-child(odd) {
            background-color: #f9fafb;
            /* Color gris claro para filas alternas */
        }

        .table th {
            background-color: #343a40;
            /* Color de fondo oscuro para encabezado */
            color: #ffffff;
            /* Texto blanco para encabezado */
            text-align: center;
            /* Centrar el texto en las celdas del encabezado */
        }

        /* Responsive design para pantallas pequeñas */
        @media (max-width: 768px) {

            .table th,
            .table td {
                font-size: 14px;
                /* Tamaño de fuente más pequeño para pantallas pequeñas */
            }

            .table tr {
                display: block;
                /* Convertir filas en bloques para mejor visualización */
                margin-bottom: 10px;
                /* Espacio entre filas en pantallas pequeñas */
            }

            .table th,
            .table td {
                display: block;
                /* Convertir celdas en bloques para mejor visualización */
                text-align: left;
                /* Alinear el texto a la izquierda en pantallas pequeñas */
            }
        }
    </style>

</head>

<body class="bg-gray-100 font-sans leading-normal tracking-normal">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/empleadosv2">Empleados</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="./usuarios.ejs">Usuarios</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container-fluid p-2">
        <h1><span>Gestión de Usuarios</span></h1>
    </div>
    <form id="formularioUsuario" class="bg-white shadow rounded p-4 mb-4">
        <input type="hidden" name="id" id="id">
        <div class="row g-3 align-items-center">
            <div class="col-auto">
                <label for="nombre" class="visually-hidden">Nombre:</label>
                <input type="text" name="nombre" id="nombre" class="form-control" placeholder="Nombre" required>
            </div>
            <div class="col-auto">
                <label for="rol" class="visually-hidden">Rol:</label>
                <select name="rol" id="rol" class="form-select" required>
                    <option value="" disabled selected>Selecciona un rol</option>
                    <option id="OptionAdmin" value="Admin">Admin</option>
                    <option value="Usuario">Usuario</option>
                </select>
            </div>
            <div class="col-auto">
                <label for="password" class="visually-hidden">Contraseña:</label>
                <input type="password" name="password" id="password" class="form-control" placeholder="Contraseña"
                    required>
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-primary">Guardar</button>
            </div>
        </div>
    </form>

    <div class="bg-white shadow rounded p-4 mb-4">
        <table class="table mb-4">
            <thead>
                <tr>
                    <th scope="col" class="text-start">ID</th>
                    <th scope="col" class="text-start">Nombre</th>
                    <th scope="col" class="text-start">Rol</th>
                    <th scope="col" class="text-start">Acciones</th>
                </tr>
            </thead>
            <tbody id="usuariosTableBody">
                <!-- Los empleados se cargarán aquí mediante AJAX -->
            </tbody>
        </table>
    </div>


    <!-- jQuery and Toastr JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous">
    </script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/toastr@2.1.4/toastr.min.js"></script>
    <!-- Logica de código JavaScript/AJAX -->
    <script src="/js/app.js?v=2"></script>
    <script src="/js/usersapp.js?v=2"></script>
</body>

</html>